{
  "name": "express-debugging-poc",
  "version": "1.0.0",
  "description": "POC for debug express server running in docker (remote)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.18.2"
  }
}
